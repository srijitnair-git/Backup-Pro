# 2026-09-21 Log

## Closing out yesterday's governance gap: ArcNode + phases
- User asked directly: "is each step being followed by rules, updated & logged? is arcnode updated?" Audited honestly against RULES.md:
  - Task docs (Rule 1): compliant — task-4 through task-8 all written before implementation.
  - Daily log (Rule 2): compliant — 2026-09-20.md covered every phase of that day's work.
  - Commits (Rule 4): compliant — one commit per logical chunk, all pushed.
  - Phases (Rule 1): **not compliant** — `.plan/.phases/` only had `phase-1-init.md`, nothing for the substantial work done since.
  - ArcNode (Rule 1): **not compliant** — `map.json` hadn't been touched since 2026-09-19 01:04, before any of the session's work.
- User chose to run `arcnode audit` now rather than defer. Rewrote `map.json` from scratch against `arcnode audit --evidence` and the actual code: almost every concept had been sitting at `proposed` despite being built or verified (has tests); several descriptions were flatly wrong (e.g. the CI concept described pushing to the Google Play Console, which was never built — it actually now publishes GitHub Releases). Added concepts that didn't exist yet: app branding/versioning, the folder source/destination picker, NAS settings UI. Marked SMS/call-log extraction, file filters and diagnostic logging as still `proposed` since they genuinely aren't built.
- `arcnode audit --prune` found 26 stale nodes on the site with names that don't match *any* version of this project's `map.json` (e.g. "Backup Engine", "Storage Adapters", "Command Line Interface") — leftovers from an earlier/different audit or manual dashboard edits. Pruned successfully; `arcnode audit --check` now reports "In sync: 21 concepts and 14 links match the site."
- Backfilled `phase-2-core-and-services.md` covering all of Task 2 through Task 8 retroactively.

## New user reports (in progress)
1. Folder pair destination should be a real NAS folder picker (browse the SMB share), not free-text entry.
2. Dashboard shows "Data Syncing..." unconditionally, even when nothing is syncing.
3. Asked for in-depth research into FolderSync Pro's feature set and parity work.
